/*
    Se solicita un programa que le pida al usuario que ingrese un numero entre 1 y 100 ( este sera el tamaño del arreglo a utilizar )

    a continuacion el programa le pide al usuario que ingrese todos los valores de la matriz,
    esto se realiza mediante la funcion:
        void ingresar_valores(float[SIZE][SIZE], int);

    la funcion
        void obtener_matriz_al_cuadrado(float[SIZE][SIZE], int);
    debera crear una matriz nueva que almacene todos los valores de la primer matriz elevados al cuadrado.

    finalmente el programa debera mostrar en pantalla ambas matrices mediante la funcion:
        void imprimir_matriz(float[SIZE][SIZE], int);
    tener en cuenta que la funcion debera llamarse una vez para cada matriz.

    Ejemplo de ejecucion:

    Ingrese la dimension del arreglo: 3
    Ingrese los valores del arreglo:
    1 2 3 5 8 10 3 6 9
    Matriz original:
    1       2       3
    5       8       10
    3       6       9

    Matriz al cuadrado:
    1       4       9
    25      64      100
    9       36      81

*/

#include <iostream>
#include <cmath>
using namespace std;
const int SIZE = 100;

int main() {

    return 0;
}
