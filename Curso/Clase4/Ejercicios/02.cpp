/*
    Se solicita un programa que sea capaz de encontrar un elemento de tipo flotante en una matriz (arreglo bidimensional)

    El programa deberá pedir el ingreso del tamaño del arreglo (arreglo bidimensional cuadrado) mediante la funcion:
        int solicitar_tamano();

    El programa pedirá el ingreso de todos los valores del arreglo mediante la funcion:
        void solicitar_numeros(float[NMAX][NMAX], int);

    El programa solicitara al usuario ingresar un valor a buscar en el arreglo.

    El programa mediante una funcion:
        void encontrar_elemento(float[NMAX][NMAX], int, float)
    debera encontrar el elemento dentro del arreglo, si este existe debera imprimir las coordenadas del mismo.

    Ejemplo de ejecucion:
    Ingrese el tamaño: 3

    Ingrese los elementos del arreglo: 1.2 3.3 5.1 2.0 4.2 6.7 2.9 8.3 11.0
    Ingrese el valor a buscar: 3.3
    El valor se encuentra en las coordenadas (0, 1)

*/

#include <iostream>
#include <cmath>
#define NMAX 100

using namespace std;

int main() {

    return 0;
}
